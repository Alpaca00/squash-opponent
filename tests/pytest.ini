[pytest]
markers =
    admin: mark tests as a admin, admin page, admin login.
    support: mark tests as a support, support database, user sent message.
    apps: mark unit test as a opponents app.
    create_image: mark tests for build app image and push to docker-hub.
